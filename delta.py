import sys
import os

size_of_set = sys.argv[1]
#interesting_function = sys.argv[2]

starting_list = list( range (int(size_of_set))
print(starting_list)

list_string = " ".join( map(str, starting_list))
print(list_string)
